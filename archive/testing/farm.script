import { getFarmThreads } from "/lib/getFarmThreads.script"

target = args[0]

tprint("Cycle time is ", tFormat(getWeakenTime(target)))
getFarmThreads(target, 0.80, 4)
getFarmThreads(target, 0.70, 4)
getFarmThreads(target, 0.50, 4)
getFarmThreads(target, 0.30, 4)

getFarmThreads(target, 0.80, 1)
getFarmThreads(target, 0.70, 1)
getFarmThreads(target, 0.50, 1)
getFarmThreads(target, 0.30, 1)