// arg[0] number of server
// arg[1] RAM, defaults to max

maxRam = getPurchasedServerMaxRam()

if (arg[1]) {
	purchaseRam = arg[1]
} else {
	purchaseRam = maxRam
}

n = 0
while (n < arg[0]) {
	if (getServerMoneyAvailable("home") > getPurchasedServerCost(ram)) {
		purchaseServer("purchasedserver", purchaseRam)
	}
	n = n + 1
}